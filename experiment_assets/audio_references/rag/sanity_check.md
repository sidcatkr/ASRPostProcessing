# Sanity Check RAG Context

- Qwen3-ASR 1.7B: ASR 모델명이다. `큐웬`, `Qwen`, `Qwen3-ASR` 표기를 구분한다.
- Qwen3.5 9B: 후처리 모델명이다.
- GPU 0번부터 3번까지: 4개 GPU 전체를 의미한다.
- DeepFilterNet2, RNNoise, BS-RoFormer: 전처리 후보 이름이다.
- 씨에스지피유투: 서버 이름 `csgpu2`를 한국어 발음으로 읽은 표현이다.
- 하드코딩 금지, 자동 실험, 청크 병합, 알에이지 검색: 실험 기능과 정책에 연결되는 핵심 표현이다.
- CER, WER, diff: UI에서 확인해야 하는 평가 항목이다.
- 18000번 포트와 7860번 포트는 서로 다른 포트이다.
